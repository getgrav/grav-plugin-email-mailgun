{
    "name": "getgrav/email-mailgun-tests",
    "description": "Test harness for the Email Mailgun plugin. Kept out of the plugin's own composer.json so that the vendor directory shipped with the plugin stays free of development packages.",
    "type": "project",
    "license": "MIT",
    "require-dev": {
        "phpunit/phpunit": "^11.5"
    },
    "autoload-dev": {
        "psr-4": {
            "Grav\\Plugin\\EmailMailgun\\": "../classes/",
            "Grav\\Plugin\\EmailMailgun\\Tests\\": "./"
        }
    },
    "config": {
        "sort-packages": true
    }
}
